import { Component, h } from '@stencil/core';

@Component({
  tag: 'ifx-feedbackWidget',
  styleUrl: 'feedbackWidget.css',
  shadow: true,
})

export class FeedbackWidget {

 
  render() {
    return (
      <div>
        test
      </div>
    );
  }
}